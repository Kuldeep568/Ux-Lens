# Frontend Environment Variables — Example
# Copy this to .env and fill in values
# .env is gitignored — never commit it

# ── API ───────────────────────────────────────────────────────────
# Development (uses Vite proxy to localhost:5000):
VITE_API_BASE_URL=/api

# Production (replace with your real Render backend URL):
# VITE_API_BASE_URL=https://ux-reviewer-backend.onrender.com/api

# ── Timeout ───────────────────────────────────────────────────────
# Time (ms) to wait for scraping + AI analysis (default 2 min)
VITE_API_TIMEOUT=120000
